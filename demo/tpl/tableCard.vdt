var card = require('/demo/tpl/card.js');

<div>
    {card.call(this, {
        // 填上children这个坑
        children: <table>
            <tr>
                <td>tableCard which required by require.js</td>
            </tr>
        </table>
    })}
</div>